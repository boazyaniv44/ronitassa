---
import BaseLayout from '../layouts/BaseLayout.astro';
import siteInfo from '../data/site-info.json';

const pageTitle = "צור קשר | רוני טסה - קצביה ברמת גן";
const pageDescription = "צרו קשר עם רוני טסה - טלפון, WhatsApp, או בואו לבקר אותנו ברחוב שלם 12 ברמת גן. נשמח לעמוד לשירותכם.";

// Format WhatsApp number for API (remove leading 0, add 972)
const whatsappNumber = siteInfo.contact.whatsapp.replace(/^0/, '972');
---

<BaseLayout title={pageTitle} description={pageDescription}>
  <!-- Hero Section -->
  <section class="relative py-16 md:py-20 overflow-hidden">
    <div class="absolute inset-0 z-0">
      <img src="/images/hero-bg.png" alt="רקע" class="w-full h-full object-cover opacity-30" />
      <div class="absolute inset-0 bg-gradient-to-b from-neutral-950/80 via-neutral-950/90 to-neutral-950"></div>
    </div>
    <div class="container relative z-10 text-center">
      <h1 class="text-5xl md:text-7xl font-bold mb-6 text-white drop-shadow-lg">
        <span class="block text-primary mb-2">צור קשר</span>
        <span class="text-2xl md:text-3xl font-light tracking-wider opacity-90">נשמח לעמוד לשירותכם</span>
      </h1>
    </div>
  </section>

  <!-- Contact Info & Hours -->
  <section class="section bg-neutral-950 text-white">
    <div class="container">
      <div class="grid md:grid-cols-2 gap-12 max-w-6xl mx-auto">
        <!-- Contact Methods -->
        <div>
          <h2 class="text-3xl font-bold mb-8 text-white flex items-center gap-3">
            <span class="text-primary">דרכי</span> התקשרות
          </h2>

          <div class="space-y-6">
            <!-- Phone -->
            <div class="flex items-start gap-6 p-6 rounded-2xl bg-white/5 border border-white/5 hover:border-primary/30 transition-all duration-300 group">
              <div class="text-4xl bg-white/10 p-3 rounded-full group-hover:scale-110 transition-transform duration-300">📞</div>
              <div>
                <h3 class="font-bold text-xl mb-2 text-accent">טלפון הקצביה</h3>
                <a href={`tel:${siteInfo.contact.phone}`} class="text-2xl font-bold hover:text-primary transition-colors block mb-1">
                  {siteInfo.contact.phone}
                </a>
                <p class="text-sm text-gray-400">זמינים בשעות הפעילות</p>
              </div>
            </div>

            <!-- WhatsApp -->
            <div class="flex items-start gap-6 p-6 rounded-2xl bg-white/5 border border-white/5 hover:border-[#25D366]/30 transition-all duration-300 group">
              <div class="text-4xl bg-white/10 p-3 rounded-full group-hover:scale-110 transition-transform duration-300">💬</div>
              <div>
                <h3 class="font-bold text-xl mb-2 text-[#25D366]">WhatsApp</h3>
                <a href={`https://wa.me/${whatsappNumber}`} class="text-2xl font-bold hover:text-[#25D366] transition-colors block mb-1" target="_blank" rel="noopener">
                  {siteInfo.contact.whatsapp}
                </a>
                <p class="text-sm text-gray-400">שלחו הודעה ונחזור אליכם בהקדם</p>
              </div>
            </div>

            <!-- Address -->
            <div class="flex items-start gap-6 p-6 rounded-2xl bg-white/5 border border-white/5 hover:border-primary/30 transition-all duration-300 group">
              <div class="text-4xl bg-white/10 p-3 rounded-full group-hover:scale-110 transition-transform duration-300">📍</div>
              <div>
                <h3 class="font-bold text-xl mb-2 text-accent">כתובת</h3>
                <p class="text-xl mb-1">{siteInfo.address.street}</p>
                <p class="text-lg text-gray-300 mb-4">{siteInfo.address.city} {siteInfo.address.zip}</p>
                <a
                  href="https://maps.app.goo.gl/kHf6CiTJqFxeuHA38"
                  target="_blank"
                  rel="noopener"
                  class="text-primary hover:text-white transition-colors inline-flex items-center gap-2 font-bold"
                >
                  פתח ב-Google Maps 
                  <span class="transform group-hover:-translate-x-1 transition-transform">←</span>
                </a>
              </div>
            </div>
          </div>

          <!-- Quick Action Buttons -->
          <div class="mt-10 flex flex-col sm:flex-row gap-4">
            <a href={`tel:${siteInfo.contact.phone}`} class="btn btn-primary text-center flex-1 py-4 text-lg shadow-lg shadow-primary/20">
              📞 התקשרו עכשיו
            </a>
            <a href={`https://wa.me/${whatsappNumber}`} class="btn bg-[#25D366] text-white hover:bg-[#128C7E] text-center flex-1 py-4 text-lg shadow-lg shadow-[#25D366]/20 border-none">
              💬 הודעה ב-WhatsApp
            </a>
          </div>
        </div>

        <!-- Hours -->
        <div>
          <h2 class="text-3xl font-bold mb-8 text-white flex items-center gap-3">
            <span class="text-primary">שעות</span> פתיחה
          </h2>

          <div class="space-y-4">
            <div class="p-6 rounded-2xl bg-white/5 border border-white/5 hover:bg-white/10 transition-colors">
              <div class="flex justify-between items-center">
                <span class="font-bold text-xl text-white">ראשון - חמישי</span>
                <span class="text-xl text-accent font-mono">{siteInfo.hours.sunday.open} - {siteInfo.hours.sunday.close}</span>
              </div>
            </div>

            <div class="p-6 rounded-2xl bg-white/5 border border-white/5 hover:bg-white/10 transition-colors">
              <div class="flex justify-between items-center">
                <span class="font-bold text-xl text-white">שישי וערבי חג</span>
                <span class="text-xl text-accent font-mono">{siteInfo.hours.friday.open} - {siteInfo.hours.friday.close}</span>
              </div>
            </div>

            <div class="p-6 rounded-2xl bg-white/5 border border-white/5 opacity-70">
              <div class="flex justify-between items-center">
                <span class="font-bold text-xl text-gray-400">שבת וחגים</span>
                <span class="text-xl text-gray-500">סגור</span>
              </div>
            </div>
          </div>

          <div class="mt-8 p-6 rounded-2xl bg-primary/10 border border-primary/20">
            <p class="text-gray-300 flex items-start gap-3">
              <span class="text-2xl">ℹ️</span>
              <span>
                <strong>שימו לב:</strong> ניתן לעשות הזמנות ב-WhatsApp מראש ולבוא לקחת בתיאום מועד
              </span>
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Map Section -->
  <section class="py-0 bg-neutral-900">
    <a 
      href="https://maps.app.goo.gl/kHf6CiTJqFxeuHA38"
      target="_blank"
      rel="noopener"
      class="block w-full h-[500px] relative grayscale hover:grayscale-0 transition-all duration-700 cursor-pointer group"
    >
      <iframe
        src="https://maps.google.com/maps?q=Shalem+12+Ramat+Gan&hl=he&z=16&output=embed"
        width="100%"
        height="100%"
        style="border:0; pointer-events: none;"
        allowfullscreen=""
        loading="lazy"
        referrerpolicy="no-referrer-when-downgrade"
        title="מפה לקצביית רוני טסה ברמת גן"
      ></iframe>
      <div class="absolute inset-0 pointer-events-none border-y-4 border-primary/50 group-hover:border-primary transition-colors"></div>
      <div class="absolute inset-0 flex items-center justify-center pointer-events-none opacity-0 group-hover:opacity-100 transition-opacity bg-black/30">
        <div class="bg-white text-black px-6 py-3 rounded-full font-bold text-lg shadow-xl">
          לחץ לפתיחה ב-Google Maps
        </div>
      </div>
    </a>
  </section>

</BaseLayout>

